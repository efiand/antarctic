<div class="firstscreen {{ bemMix }}" {{ mixin.background(image, null, true) }}>
	<div class="container">
		{% include 'page-header.twig' with { bemMix: 'firstscreen__header' } %}

		<section class="firstscreen__promo">
			{{ content }}
		</section>
	</div>
</div>
