<ul class="nav {{ bemMix }} {{ bemModifier ? 'nav--' ~ bemModifier }}">
	{% for link in navLinks %}
		<li>
			<a href="{{ page != 'index' ? 'index.html' }}{{ link.url }}" class="nav__link">
				{{ link.title }}
			</a>
		</li>
	{% endfor %}
</ul>
